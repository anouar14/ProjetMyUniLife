{% extends 'depense.html.twig' %}

{% block title %}New Categorie{% endblock %}

{% block body %}
    <h1>Create new Categorie</h1>

    {{ include('categorie/_form.html.twig') }}
    {% for message in app.flashes('error') %}
    <div style="color: red; font-weight: bold; margin-bottom: 10px;">
        {{ message }}
    </div>
    {% endfor %}
    <a href="{{ path('app_categorie_index') }}" class="btn hero-btn" data-animation="fadeInLeft" >Back to List</a>
{% endblock %}
