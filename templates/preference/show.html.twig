{% extends 'base.html.twig' %}

{% block title %}Preference{% endblock %}

{% block body %}
    <h1>Preference</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preference.id }}</td>
            </tr>
            <tr>
                <th>TypePr</th>
                <td>{{ preference.typePr }}</td>
            </tr>
            <tr>
                <th>DisponibiliteAC</th>
                <td>{{ preference.disponibiliteAC ? preference.disponibiliteAC|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_preference_index') }}">back to list</a>

    <a href="{{ path('app_preference_edit', {'id': preference.id}) }}">edit</a>

    {{ include('preference/_delete_form.html.twig') }}
{% endblock %}
