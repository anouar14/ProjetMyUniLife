<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Supprimer l'Activité</title>
</head>

<body>

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-header bg-danger text-white">
                        <h4 class="mb-0">Confirmation de Suppression</h4>
                    </div>
                    <div class="card-body">
                        <!-- Message générique de confirmation de suppression -->
                        <p>Êtes-vous sûr de vouloir supprimer cette activité ?</p>
                        <p class="text-muted">Cette action est irréversible. Une fois supprimée, l'activité ne pourra pas être récupérée.</p>

                        <!-- Formulaire de confirmation -->
                        <form method="post" action="{{ path('app_activite_delete', {'id': activite.id}) }}">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite.id) }}">

                            <div class="d-flex justify-content-between">
                                <button type="submit" class="btn btn-danger btn-lg">Supprimer</button>
                                <a href="{{ path('app_activite_index') }}" class="btn btn-secondary btn-lg">Annuler</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

    </div>
{% endblock %}

</body>

</html>
