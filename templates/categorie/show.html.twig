{% extends 'depense.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>NomCat</th>
                <td>{{ categorie.nomCat }}</td>
            </tr>
            <tr>
                <th>MontanMax</th>
                <td>{{ categorie.montanMax }}</td>
            </tr>
            <tr>
                <th>MontantCat</th>
                <td>{{ categorie.montantCat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}" class="btn hero-btn" data-animation="fadeInLeft" >Back to List</a>

    <a href="{{ path('app_categorie_index', {'id': categorie.id}) }}" class="btn hero-btn" data-animation="fadeInLeft" >Edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
